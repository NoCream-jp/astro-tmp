---
import PostCard from '@/components/PostCard.astro';
import { getCollection } from 'astro:content';

// blog コレクションを取得
const posts = await getCollection("blog");

// slug の降順（新しいものが上に来る想定）でソート
posts.sort((a: { slug: any; }, b: { slug: string; }) => b.slug.localeCompare(a.slug));

// 最新記事のURLを生成（記事がない場合はトップページへ）
const latestPostUrl = posts.length > 0 ? `/blog/${posts[0].slug}/` : '/';

// 記事の数を制限
const limitedPosts = posts.slice(0, 6);
const hasMorePosts = posts.length > 6;
---

<html lang="ja">

    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>DripNotes</title>
        <link rel="stylesheet" href="/styles/index.css">
        <link rel="preconnect" href="https://fonts.gstatic.com">
        <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Monsterrat:wght@300;400&display=swap">
        <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.15.3/css/all.css">
        <link rel="preconnect" href="https://fonts.googleapis.com">
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
        <link href="https://fonts.googleapis.com/css2?family=Dancing+Script:wght@400..700&display=swap" rel="stylesheet">
        <link rel="preconnect" href="https://fonts.googleapis.com">
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
        <link href="https://fonts.googleapis.com/css2?family=Noto+Sans+JP:wght@400;700&display=swap" rel="stylesheet">
    </head>

    <body>
        <header class="header">
            <div class="header-container">
                <div class="header-left">
                    <a href="/" class="header-icon-link" aria-label="ホーム"><i class="fas fa-home"></i></a>
                </div>
                <div class="header-right">
                    <a href="/" class="header-icon-link" aria-label="プロフィール"><i class="fas fa-user"></i></a>
                    <a href="/" class="header-icon-link" aria-label="ホーム"><i class="fas fa-book"></i></a>
                    <a href="/" class="header-icon-link" aria-label="制作物"><i class="fas fa-tools"></i></a>
                    <a href="/" class="header-icon-link share" aria-label="共有"><i class="fas fa-share-square"></i></a>
                </div>
                <button class="nav-button">
                    <i class="fas fa-bars"></i>
                </button>
        </header>

        <main>
            <section class="profile"></section>
            <section class="blog"></section>
            <section class="works"></section>
        </main>

        <script src="/scripts/indexScript.js" defer></script>
    </body>

</html>